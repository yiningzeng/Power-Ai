{
    "type": "object",
    "properties": {
        "name": {
            "title": "${strings.common.displayName}",
            "type": "string",
            "pattern": "^[^\\\\\\\\/:*?\\\\\\\"<>|]*$"
        }
    },
    "required": [
        "name"
    ]
}
